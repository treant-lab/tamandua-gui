[package]
name = "tamandua-gui-visual-fixture"
version = "0.1.0"
description = "Isolated Tamandua Windows visual fixture canary"
edition = "2021"
publish = false

[build-dependencies]
tauri-build = { version = "=1.5.6", features = [] }

[dependencies]
tauri = { version = "=1.8.3", default-features = false, features = ["custom-protocol", "wry"] }

[features]
default = ["visual-fixture"]
visual-fixture = []
production = []
